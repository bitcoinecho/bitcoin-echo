---
phase: 01-foundation-fixes
plan: 07
type: execute
wave: 4
depends_on:
  - 01-05
files_modified:
  - test/unit/test_download_mgr.c
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "Peer eviction correctly disconnects the slowest peers when above minimum peer count"
    - "Peer eviction does NOT disconnect peers when at or below minimum peer count"
    - "Eviction selects the correct peers based on download rate comparison"
  artifacts:
    - path: "test/unit/test_download_mgr.c"
      provides: "Peer eviction test cases under various load scenarios"
      min_lines: 50
  key_links:
    - from: "test/unit/test_download_mgr.c"
      to: "src/protocol/download_mgr.c (eviction functions)"
      via: "Tests call download_mgr_check_performance and download_mgr_evict_slowest_percent"
      pattern: "download_mgr_evict_slowest_percent\\|download_mgr_check_performance"
---

<objective>
Add test coverage for peer eviction under load: multiple peers at varying simulated rates, minimum peer count enforcement, and correct slowest-peer selection.

Purpose: Peer eviction logic has multiple interacting thresholds (minimum rate, minimum peer count, stall timeout). Tests validate that the eviction functions correctly identify slow peers, respect the minimum peer count floor, and disconnect the right peers. This prevents network isolation bugs where too many peers are evicted.

Output: Extended test_download_mgr.c with eviction test suite.
</objective>

<execution_context>
@/Users/yayseth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yayseth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-fixes/01-RESEARCH.md
@.planning/phases/01-foundation-fixes/01-05-SUMMARY.md
@include/download_mgr.h
@test/unit/test_utils.h
@test/unit/test_download_mgr.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add peer eviction test suite to test_download_mgr.c</name>
  <files>test/unit/test_download_mgr.c</files>
  <action>
**Read existing test infrastructure first:**

1. Read `test/unit/test_download_mgr.c` to understand existing mock peer setup, `mock_peer_t` struct, callback infrastructure (`mock_send_getdata`, `mock_disconnect_peer`), and test context pattern
2. Read `include/download_mgr.h` for the exact API signatures of `download_mgr_check_performance()`, `download_mgr_evict_slowest_percent()`, and related functions. Find `DOWNLOAD_MIN_PEERS_TO_KEEP` constant.
3. Understand how peer download rates are tracked — look for bytes_received, time_window, or rate calculation functions

**Add eviction test functions** following existing mock patterns:

**Test 1: Evict slowest peers when above minimum count**

Set up a download manager with 8 mock peers. Simulate download activity:
- 4 "fast" peers: simulate high bytes_received values (e.g., 100 KB in the time window)
- 4 "slow" peers: simulate low bytes_received values (e.g., 100 bytes in the time window)

Call `download_mgr_evict_slowest_percent()` (or `download_mgr_check_performance()` — use whichever is the public eviction trigger). Verify:
- Slow peers received disconnect callback
- Fast peers were NOT disconnected
- Total remaining peers >= DOWNLOAD_MIN_PEERS_TO_KEEP

```c
static void test_evict_slowest_above_min(void) {
    test_case("evict slowest peers when above minimum count");
    /* Setup 8 mock peers with varying rates */
    /* Trigger eviction */
    /* Verify slow peers disconnected, fast peers kept */
}
```

**Test 2: Do NOT evict when at minimum peer count**

Set up a download manager with exactly `DOWNLOAD_MIN_PEERS_TO_KEEP` peers (likely 3), all slow. Call eviction trigger. Verify:
- NO peers disconnected (minimum count protection)
- All peers still present

```c
static void test_no_evict_at_minimum(void) {
    test_case("no eviction when at minimum peer count");
    /* Setup exactly DOWNLOAD_MIN_PEERS_TO_KEEP slow peers */
    /* Trigger eviction */
    /* Verify zero disconnections */
}
```

**Test 3: Eviction respects minimum — evicts some but stops at floor**

Set up 5 peers: 4 slow, 1 fast. If DOWNLOAD_MIN_PEERS_TO_KEEP=3, eviction should disconnect at most 2 slow peers (5-2=3 remaining). Verify:
- At most 2 slow peers disconnected
- At least DOWNLOAD_MIN_PEERS_TO_KEEP peers remain
- The fast peer is NOT disconnected

**Test 4: All peers at same rate — no eviction (no clear slowest)**

Set up 6 peers all at identical simulated rates. Trigger eviction. Behavior depends on implementation — either no eviction (all equal) or random selection. Verify no crash, no incorrect state.

**Test 5: Stalled peer (zero bytes received)**

Set up 6 peers, one with zero bytes_received over the time window. Trigger eviction. Verify the stalled peer is evicted first (zero-rate is lowest).

**Mock rate simulation approach:**

Read how the download manager tracks peer rates. It likely uses `bytes_received` and a time window. To simulate rates:
- After adding a mock peer, directly set the peer's `bytes_received` field (if accessible) or call the function that records bytes received with a controlled amount
- Advance the time tracking (may need to call `plat_time_ms()` or set a mock time value)
- If rate tracking is internal and not directly settable, read the code to find the injection point

**Follow existing test patterns:** Use `test_case()`, `test_pass()`, `test_fail()` from test_utils.h. Use the same mock callback infrastructure already in the file.

**Wire new tests into main():** Add calls to all new test functions in the `main()` function of test_download_mgr.c, after existing test calls.
  </action>
  <verify>
Run `make test` — test_download_mgr passes all new eviction tests. Test output shows each eviction test case by name. Verify no test hangs. Grep test_download_mgr.c for `test_evict` — new test functions present. Verify tests use the existing mock infrastructure (mock_peer_t, mock_disconnect_peer).
  </verify>
  <done>
Peer eviction test suite covers: slowest eviction above minimum count, no eviction at minimum count, eviction stops at minimum floor, equal-rate peers, and stalled peer eviction. All tests use existing mock peer infrastructure and follow test_utils.h patterns.
  </done>
</task>

</tasks>

<verification>
1. `make` compiles with zero warnings
2. `make test` — all tests pass including new eviction tests
3. Test output shows 5 new eviction test cases passing
4. No test deadlocks or hangs
5. Tests validate both eviction behavior AND minimum peer count protection
</verification>

<success_criteria>
- Eviction tests verify correct peer selection by download rate
- Minimum peer count floor is enforced (no eviction below threshold)
- Stalled peer (zero rate) is evicted first
- All tests use existing mock infrastructure — no real network access
- Tests follow existing test_utils.h patterns (test_case, test_pass, test_fail)
- Zero compiler warnings, all existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-fixes/01-07-SUMMARY.md`
</output>
