---
phase: 04-bip125-full-rbf-mempool
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - test/unit/test_mempool.c
autonomous: true
requirements:
  - TEST-01

must_haves:
  truths:
    - "All 5 BIP-125 rules are individually exercised by dedicated test cases"
    - "Rule 3 absolute-fee trap is tested — higher feerate but lower absolute fee is rejected"
    - "Inherited RBF signaling propagation to descendants is tested"
    - "Eviction count limit (100) is tested"
    - "Successful multi-conflict replacement is tested"
  artifacts:
    - path: "test/unit/test_mempool.c"
      provides: "BIP-125 RBF test section with 8 test functions"
      contains: "BIP-125 RBF Replacement Rules"
  key_links:
    - from: "test/unit/test_mempool.c RBF tests"
      to: "mempool_add() RBF path"
      via: "create conflicting transactions and assert accept/reject behavior"
      pattern: "mempool_add.*MEMPOOL_REJECT_RBF"
    - from: "test RBF section in main()"
      to: "test_rbf_* functions"
      via: "test_section + test_case + test_pass wiring"
      pattern: "test_section.*BIP-125"
---

<objective>
Add comprehensive BIP-125 RBF test coverage to test_mempool.c exercising all 5 replacement rules.

Purpose: The phase success criteria require that "all 5 replacement rules are exercised by the test suite, including inherited RBF signaling propagation to descendants and the 100-transaction eviction count limit." This plan adds 8 test functions covering every rule, edge cases, and successful replacement scenarios.

Output: A new "BIP-125 RBF Replacement Rules" section in test_mempool.c with 8 test functions wired into main().
</objective>

<execution_context>
@/Users/yayseth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yayseth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bip125-full-rbf-mempool/04-RESEARCH.md
@.planning/phases/04-bip125-full-rbf-mempool/04-01-SUMMARY.md
@include/mempool.h
@test/unit/test_mempool.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RBF test helper and test functions for Rules 1-3</name>
  <files>test/unit/test_mempool.c</files>
  <action>
Add a new helper and the first batch of test functions BEFORE main() in test_mempool.c:

1. **Helper: `create_test_tx_no_rbf()`** — Same as `create_test_tx()` but sets `sequence = 0xFFFFFFFF` (RBF disabled). This creates transactions that do NOT signal RBF, needed for Rule 1 tests.

2. **`test_rbf_rule1_signaling()`** — Rule 1: original must signal RBF.
   - Add mock UTXO (make_txid(300), vout 0, value 100000)
   - Create original tx using `create_test_tx_no_rbf()` spending that UTXO with output 90000 (10000 sat fee). This original does NOT signal RBF (sequence 0xFFFFFFFF).
   - Add original to mempool — should succeed.
   - Create replacement using `create_test_tx()` (which DOES signal) spending same input with output 80000 (20000 sat fee, higher).
   - Call `mempool_add(mp, &replacement, &result)` — must return `ECHO_ERR_INVALID` with `result.reason == MEMPOOL_REJECT_CONFLICT` (Rule 1: original doesn't signal).
   - Clean up.

3. **`test_rbf_rule1_inherited()`** — Rule 1: inherited signaling.
   - Add mock UTXO (make_txid(301), vout 0, value 100000).
   - Create parent tx using `create_test_tx()` spending that UTXO with output 90000. Parent signals RBF. Add to mempool.
   - Get parent's txid (compute via tx or look up).
   - Create child tx using `create_test_tx_no_rbf()` spending parent's output (parent_txid, vout 0) with output 80000. Child does NOT directly signal but inherits from parent.
   - To make child spendable from mempool: child's input is the parent's output (unconfirmed). Add child to mempool — should succeed (parent is in mempool, child can spend its outputs).
   - Create replacement for child spending same input (parent_txid, vout 0) with higher fee. Use `create_test_tx()`.
   - `mempool_add(mp, &replacement, &result)` — must return `ECHO_OK` (child inherits signaling from parent, so Rule 1 passes).
   - Verify the child is no longer in mempool (evicted) and replacement is present.
   - Clean up.

4. **`test_rbf_rule2_no_new_unconfirmed()`** — Rule 2: no new unconfirmed inputs.
   - Add two mock UTXOs: make_txid(302) and make_txid(303).
   - Create original spending make_txid(302). Add to mempool.
   - Create an unrelated "parent" tx spending make_txid(303). Add to mempool. This creates an unconfirmed output.
   - Create replacement with 2 inputs: (1) same input as original (make_txid(302), vout 0), (2) an output from the unrelated parent (unconfirmed, not in eviction set).
   - `mempool_add(mp, &replacement, &result)` — must return `ECHO_ERR_INVALID` with `result.reason == MEMPOOL_REJECT_CONFLICT` (Rule 2: new unconfirmed input not from eviction set).
   - Clean up.

5. **`test_rbf_rule3_absolute_fee_trap()`** — Rule 3: absolute fee, not feerate.
   - Add mock UTXO (make_txid(304), vout 0, value 100000).
   - Create original with output 95000 (5000 sat fee). Add to mempool.
   - Create replacement with output 95001 (4999 sat fee — lower absolute fee, possibly higher feerate if smaller).
   - `mempool_add(mp, &replacement, &result)` — must return `ECHO_ERR_INVALID` with `result.reason == MEMPOOL_REJECT_RBF_INSUFFICIENT_FEE`.
   - This is THE critical edge case: higher feerate but lower absolute fee must be rejected.
   - Clean up.

For multi-input replacement tests (Rule 2), create a helper `create_test_tx_2in()` that builds a tx with 2 inputs and 1 output. Both inputs signal RBF (sequence 0xFFFFFFFE).
  </action>
  <verify>
`make test_mempool` — all existing tests plus the 4 new RBF tests pass. Each test exercises a specific BIP-125 rule boundary.
  </verify>
  <done>
`create_test_tx_no_rbf`, `create_test_tx_2in`, and 4 test functions (rule1_signaling, rule1_inherited, rule2_no_new_unconfirmed, rule3_absolute_fee_trap) exist in test_mempool.c and pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add test functions for Rules 4-5, success cases, and wire into main</name>
  <files>test/unit/test_mempool.c</files>
  <action>
Add the remaining test functions and wire everything into main():

1. **`test_rbf_rule4_fee_rate()`** — Rule 4: fee rate increment.
   - Add mock UTXO (make_txid(305), vout 0, value 100000).
   - Create original with output 90000 (10000 sat fee). Add to mempool.
   - Compute the minimum required fee for Rule 4: `original_fee + (MEMPOOL_RBF_INCREMENT * replacement_vsize / 1000)`. Since MEMPOOL_RBF_INCREMENT is 1000 (1 sat/vB) and a test tx is roughly 85-100 vB, the increment is ~85-100 sats.
   - Create replacement with fee just below the Rule 4 threshold. For example, if original fee is 10000 and increment is ~85 sats, set replacement output so fee = 10050 (below 10085). This should fail Rule 4.
   - Assert `ECHO_ERR_INVALID` with `MEMPOOL_REJECT_RBF_INSUFFICIENT_FEE`.
   - Then create a second replacement with fee above the threshold (e.g., output 89800, fee 10200) — should succeed.
   - Clean up.

2. **`test_rbf_rule5_eviction_limit()`** — Rule 5: eviction count limit (100).
   - Add mock UTXO (make_txid(306), vout 0, value 10000000 — large enough for chain).
   - Create a chain of transactions: first tx spends the UTXO, each subsequent tx spends the previous tx's output. Create a chain longer than MEMPOOL_MAX_REPLACEMENT_COUNT (101 transactions total including the root).
   - Build each tx with slightly less output value (to pay fees). Each signals RBF.
   - Add all to mempool. The chain has 101 entries total (1 root + 100 descendants).
   - Create replacement spending the same UTXO as the root (make_txid(306), vout 0). Higher fee.
   - `mempool_add(mp, &replacement, &result)` — must return `ECHO_ERR_INVALID` with `result.reason == MEMPOOL_REJECT_RBF_TOO_MANY_REPLACED`.
   - Clean up.
   - Note: MEMPOOL_MAX_ANCESTORS (25) might limit chain depth before reaching 101. If so, build a wider tree instead: one root with multiple children each spending a different output. Adjust the root to have enough outputs (give it output_count > 1) or use multiple roots conflicting on different inputs. The simplest approach: create multiple independent conflict chains that share the same single UTXO input. But since they all spend the same UTXO, only one can exist. Alternative: create the root with many outputs (e.g., 50 outputs), then create 2 children per output = 100 descendants + 1 root = 101 total. This requires a `create_test_tx_multi_output()` helper.

3. **`test_rbf_success()`** — Successful single replacement.
   - Add mock UTXO (make_txid(307), vout 0, value 100000).
   - Create original with output 90000 (10000 sat fee, signals RBF). Add to mempool.
   - Create replacement with output 80000 (20000 sat fee, signals RBF, same input).
   - `mempool_add(mp, &replacement, &result)` — must return `ECHO_OK`.
   - Verify original is no longer in mempool (`mempool_exists` returns false for original txid).
   - Verify replacement IS in mempool.
   - Verify `mempool_size(mp) == 1` (replaced, not added alongside).
   - Clean up.

4. **`test_rbf_multi_conflict()`** — Multi-conflict eviction.
   - Add two mock UTXOs: make_txid(308) and make_txid(309).
   - Create original1 spending make_txid(308). Add to mempool.
   - Create original2 spending make_txid(309). Add to mempool.
   - Create replacement with 2 inputs spending BOTH make_txid(308) and make_txid(309). Higher total fee than both originals combined. Use `create_test_tx_2in()`.
   - `mempool_add(mp, &replacement, &result)` — must return `ECHO_OK`.
   - Verify both originals are gone, replacement is in mempool.
   - Verify `mempool_size(mp) == 1`.
   - Clean up.

5. **Wire into main()** — Add a new section after the existing test cases in `main()`:
```c
test_section("BIP-125 RBF Replacement Rules");
test_case("RBF Rule 1: signaling required"); test_rbf_rule1_signaling(); test_pass();
test_case("RBF Rule 1: inherited signaling"); test_rbf_rule1_inherited(); test_pass();
test_case("RBF Rule 2: no new unconfirmed inputs"); test_rbf_rule2_no_new_unconfirmed(); test_pass();
test_case("RBF Rule 3: absolute fee trap"); test_rbf_rule3_absolute_fee_trap(); test_pass();
test_case("RBF Rule 4: fee rate increment"); test_rbf_rule4_fee_rate(); test_pass();
test_case("RBF Rule 5: eviction count limit"); test_rbf_rule5_eviction_limit(); test_pass();
test_case("RBF: successful replacement"); test_rbf_success(); test_pass();
test_case("RBF: multi-conflict eviction"); test_rbf_multi_conflict(); test_pass();
```

Place this section BEFORE `test_suite_end()` and AFTER the existing `test_case("Reject coinbase")` line.
  </action>
  <verify>
`make test_mempool` — all tests pass (24 original + 8 new = 32 total). Every BIP-125 rule has at least one dedicated test. The absolute-fee trap (Rule 3) specifically tests the edge case from Success Criteria 3. Inherited signaling (Rule 1) tests the propagation case from Success Criteria 4.
  </verify>
  <done>
8 RBF test functions exist and pass. test_section("BIP-125 RBF Replacement Rules") appears in main(). All 5 BIP-125 rules are exercised including: absolute-fee trap (Rule 3), inherited signaling (Rule 1), eviction count limit of 100 (Rule 5), successful replacement, and multi-conflict eviction.
  </done>
</task>

</tasks>

<verification>
1. `make test_mempool` passes with all 32 tests (24 original + 8 new)
2. Rule 1 tested: non-signaling original rejected, inherited signaling accepted
3. Rule 2 tested: new unconfirmed input from outside eviction set rejected
4. Rule 3 tested: 4999-sat replacement against 5000-sat original rejected (absolute fee trap)
5. Rule 4 tested: replacement below fee rate increment threshold rejected
6. Rule 5 tested: eviction set exceeding 100 transactions rejected
7. Success case tested: valid replacement accepted, original evicted
8. Multi-conflict tested: replacement evicting 2 originals succeeds
</verification>

<success_criteria>
- test_mempool.c contains 8 new test functions in a "BIP-125 RBF Replacement Rules" section
- All 5 BIP-125 rules have dedicated test cases with correct assertions
- The absolute-fee trap edge case is specifically tested (Rule 3)
- Inherited signaling propagation is tested (Rule 1)
- Eviction count limit of 100 is tested (Rule 5)
- All tests pass when run via `make test_mempool`
- No existing tests are broken
</success_criteria>

<output>
After completion, create `.planning/phases/04-bip125-full-rbf-mempool/04-02-SUMMARY.md`
</output>
